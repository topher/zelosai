// generateSubscription.ts

import { logDefaultSubscription } from './lib/subscription';

const newUserId = 'user_12345'; // Replace with the actual user ID
const subscriptionTier = 'FREE'; // Replace with desired tier if different

logDefaultSubscription(newUserId);
